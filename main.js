// post  | per  increment

/*
If the variable and before it is ++, it means add and then display the new number
If ++ is after the variable, it means print the variable and then display the variable
 */
